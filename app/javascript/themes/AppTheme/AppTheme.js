import { createTheme } from '@material-ui/core/styles';

const AppTheme = createTheme({
  palette: {
    primary: {
      main: '#0052CC',
    },
    secondary: {
      main: '#D32F2F',
    },
  },
});

export default AppTheme;
